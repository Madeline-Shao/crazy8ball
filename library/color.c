#include <stdlib.h>
#include <assert.h>
#include "color.h"

typedef struct rgb_color rgb_color_t;

/*
rgb_color_t *color_init(float red, float green, float blue){
    rgb_color_t *new_color = malloc(sizeof(rgb_color_t));
    assert(new_color != NULL);
    new_color->r = red;
    new_color->g = green;
    new_color->b = blue;

    return new_color;
}
*/
